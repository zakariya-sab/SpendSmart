/**
 * Development environment configuration.
 * The apiUrl points to the local Spring Boot backend on port 8080.
 */
export const environment = {
  production: false,
  /** Base URL for all Spring Boot API calls */
  apiUrl: 'http://localhost:8080'
};
