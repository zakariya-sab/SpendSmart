/**
 * Production environment configuration.
 * Update apiUrl to point to the deployed Spring Boot backend.
 */
export const environment = {
  production: true,
  /** Base URL for all Spring Boot API calls in production */
  apiUrl: 'http://localhost:8080'
};
